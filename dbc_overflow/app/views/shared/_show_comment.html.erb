<p><%= comment.body %></p>
<% if @user == comment.user_id %>
  <%= link_to 'Edit comment', polymorphic_url([comment.commentable, comment], action: 'edit') %>
  <%= link_to 'Delete comment', comment, method: 'delete' %>
<% end %>